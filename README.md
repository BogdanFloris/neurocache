# neuro

Implementation of a `etcd` like KV store using the Raft consensus algorithm. Contains the Raft library implementation and 2 CLI applications:

- `neurod` - which starts a Raft node
- `neuroctl` - client CLI
